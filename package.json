{
  "name": "mcp-fig",
  "version": "0.0.0",
  "description": "A compact MCP server for structured Figma operations and reliable Auto Layout.",
  "type": "module",
  "private": true,
  "bin": {
    "mcp-fig": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "quality": "vitest run tests/quality-gates.test.ts",
    "snapshots:update": "npm run build && node scripts/update-quality-snapshots.mjs",
    "smoke": "npm run build && node scripts/smoke-mcp.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@types/node": "^25.0.10",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "@hono/node-server": "^2.0.11"
  }
}
